{% extends 'base.html.twig' %}
{% block title %}Login Page{% endblock %}

{% block body %}
<img src="images/logo.png" class="logo" >
<h1>{{ title }}</h1>

{#style="width:20%"#}
<div class="alignbutton">
    <button class = "button"><a href=" {{ path('annoncespage') }}">Annonces</a></button>
    <button class = "button"><a href=" {{ path('homepage') }}">Deconnexion</a></button>

</div>
<form action="{{ path('createannoncepage') }}" method="post">
    <button type="submit" class = "blueButton" style="margin-top: 200px">Créer une annonce</button>
</form>

<div class="menu">



    {% endblock %}